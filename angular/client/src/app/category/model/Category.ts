// Para manejar la info del listado de Categorías almacenamos los datos en un objeto de tipo model.
// Esta clase la utilizaremos en el código html y ts del componente.
export class Category {
    id: number;
    name: string;
}
